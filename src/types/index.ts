export interface Artesao {
  id: string;
  nome: string;
  cidade: string;
  estado: string;
  imagemUrl: string;
}

export interface Peca {
  id: string;
  nome: string;
  preco: number;
  imagemUrl: string;
}